const Calculator = {
  multiply: function(x, y) {
    return x * y;  
  },
  add: function(x, y) {
    return x + y;
  },
  subtract: function(x, y) {
    return x - y;
  },
  divide: function(x,y) {
    return x/y;
  }
};

export default Calculator;